import GameView

class TestGameView:
    def test_display_boards():
        view = GameView()
        result = view.display_boards(player1)
        assert result == "boards_displayed"

    def test_get_player_input():
        view = GameView()
        input_coord = simulate_user_input()
        assert input_coord is not None
