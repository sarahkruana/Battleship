class GameView:
    def display_boards(player):
        show_player_board(player)
        show_opponent_model_board(player)

    def get_player_input():
        ask_user_for_coordinates()
        return coordinates

    def refresh_display(model):
        clear_screen()
        display_boards(current_player)

    def show_winner(player):
        print(player, "wins!")
